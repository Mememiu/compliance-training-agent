# 冷色一体化本地预览

用户于 2026-09-09 确认预览上一轮提出的方案：全站浅灰蓝 #F5F7FC、白色内容面、靛紫 #4F46E5 操作色、深蓝墨 #192C48；微课页压缩顶部导航留白。保留首页彩色课程卡、文字内容、字号特征、布局和动画，微课源与学习逻辑不变。

本次仅本地预览，不提交、不推送、不部署。旧设计文档保留为线上基线，新方向待用户确认后再合并品牌规范。

使用独立 learning-theme.css 映射共享语义 token，避免更改自动生成旧 tokens。全站主题共同生效，不仅改变页头背景。微课桌面顶栏使用对称三列保证胶囊在视口中心，移动端保持两行以防挤压。深色主题单独指定前景与操作色，避免直接套用浅色导致对比度降低。
