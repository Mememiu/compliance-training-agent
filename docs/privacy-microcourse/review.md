# 接入审查（2026-09-08）

## 规格审查

独立审查已确认：文字按顺序显式确认；微课前置条件、旧测验路由重定向和通用更新接口限制同时生效；六区隐患及自测共同决定完成；其他课程保留原考核。原微课源项目没有修改，素材和动画以终版 HTML 导入。

## 代码质量审查

- Critical：无未解决项。
- Major：无未解决项。
- Minor：原微课将“已找到全部隐患”称为“通关”，包括读屏播报。已在导入器中精确替换为“已排查／隐患已找齐”，真实保存自测后才显示通关；增加回归测试。
- 关联修复：首页 `useMemo` 原先位于条件 return 之后。移动到固定 hook 顺序，避免学习完成后返回首页触发 hook 数量错误；未改动卡组动画或排布。

## 验证方式

单元测试覆盖迁移、阅读顺序、重复／未知隐患、缺失自测、完成后重温、消息来源和 token、桥接保存失败、重置、原版动画标记及文案。

隔离浏览器完成桌面 1440×1000 与手机 390×844 的真实六区操作：读六课、进入大楼、点击十八处隐患、六次错答与正确作答、未答对时禁用继续按钮、第一间自测前刷新并续学、最后通关、刷新后完成状态、课程目录和首页状态。没有写入用户正常浏览器的测试数据。

恢复场景覆盖文字保存失败与重试、旧成绩保留、外层保存失败与重新同步、伪造消息、微课双击确认重置后的持久完成、另外两门课程提交原测验、损坏 JSON 不被覆盖。

## 明确边界

沿用当前浏览器 localStorage，不是服务端考核凭证、不支持自动跨设备同步；同源消息校验避免误收其他窗口消息，不宣称抵抗用户主动修改本地数据。清空站点数据会清除当前浏览器学习进度。已获得的新版模块完成记录不因重温时重置微课而撤销。

生产构建有原有大 bundle 的大小提示，非编译错误。微课约 5.7 MiB，作为单独静态资源仅在进入微课时加载。动画完整视觉回归仅覆盖测试设备尺寸，不声称测试了所有设备。
