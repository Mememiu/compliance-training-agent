# 接入审查（2026-09-09）

## 规格与代码审查

独立审查确认：第二课《多停一秒·尊重有界》V1 接入劳动合规与职场行为；两个文字课时须显式顺序确认，三段情境须完整听取、选择认可回应并点击「记住这次回应」，持久保存成功后才计入完成。历史测验只作为历史，反腐败课程保留原测验。第一课仍使用原有状态与素材，宿主入口通过小型注册表共用。

发现并解决一项 P2：微课记录读取／解析失败后，首次 render 可能保存空记录覆盖旧数据。修复为独立 restorationFailed 锁存；成功明确恢复前禁止 save、complete 和空 state 发布，保留原始记录与错误提示。普通写入失败仍可原地重试。回归测试包含真实 restore → 初次 render/save → init 顺序。独立复审通过，无剩余阻塞项。

注册表增加 own-key 限制，非法 constructor 路由不能误识别为微课配置。没有修改原始微课工程、插画、对话、动画、顶栏或卡组排布。

## 验证

- 全部 37 项单元测试通过，包括第一课、第二课、卡组、课程合并、迁移、消息来源／nonce、认可回应、刷新恢复与存储故障。
- TypeScript 和 Vite 生产构建通过。现有大 bundle 大小提示仍在，非编译错误。
- 生产静态预览使用与 Pages 一致的子路径；1440×1000 和 390×844 隔离 Chrome 上真实完成第二课两文字课与三情境，覆盖错误回应重试、未确认不能完成、对话／反馈刷新续学、写失败不解锁、完成后刷新与重播、目录及首页。无页面异常、无横向溢出。
- 同尺寸完整回归第一课六文字、十八隐患、六区错答及正确作答与完成恢复。独立回归历史成绩、宿主保存失败重试、伪造消息、第一课重置、反腐败测验、损坏宿主 JSON 保护。

## 边界

当前进度依旧只保存在本浏览器 localStorage，不提供跨设备同步或防用户主动修改的考核凭证。来源文档和哈希见 source.md。截图与浏览器测试数据只在临时目录／隔离上下文，不写入正常用户浏览器，也不发布到站点。
