/**
 * tdesign v0.1.0-alpha.15
 * (c) 2025 tdesign
 * @license MIT
 */

import _slicedToArray from '@babel/runtime/helpers/slicedToArray';
import { useRef, useEffect } from 'react';

var useDynamicStyle = function useDynamicStyle(elementRef, cssVariables) {
  var styleId = useRef("dynamic-styles-".concat(Math.random().toString(36).slice(2, 11)));
  var generateScopedStyles = function generateScopedStyles(vars) {
    var variables = Object.entries(vars).map(function (_ref) {
      var _ref2 = _slicedToArray(_ref, 2),
        key = _ref2[0],
        value = _ref2[1];
      return "".concat(key, ": ").concat(value, ";");
    }).join("\n");
    return "\n      .".concat(styleId.current, " {\n        ").concat(variables, "\n      }\n    ");
  };
  useEffect(function () {
    if (!(elementRef !== null && elementRef !== void 0 && elementRef.current)) return;
    var styleElement = document.createElement("style");
    styleElement.innerHTML = generateScopedStyles(cssVariables);
    document.head.appendChild(styleElement);
    var currentElement = elementRef.current;
    if (currentElement) {
      currentElement.classList.add(styleId.current);
    }
    return function () {
      document.head.removeChild(styleElement);
      if (currentElement) {
        currentElement.classList.remove(styleId.current);
      }
    };
  }, [cssVariables]);
};

export { useDynamicStyle };
//# sourceMappingURL=useDynamicStyle.js.map
