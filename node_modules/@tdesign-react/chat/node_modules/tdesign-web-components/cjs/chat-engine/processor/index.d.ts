import type { AIMessage, AIMessageContent, AttachmentItem, ChatMessagesData } from '../type';
export default class MessageProcessor {
    private contentHandlers;
    constructor();
    createUserMessage(content: any, attachments?: AttachmentItem[]): ChatMessagesData;
    createAssistantMessage(msg?: {
        content?: AIMessageContent[];
        status?: AIMessage['status'];
    }): AIMessage;
    processContentUpdate(lastContent: AIMessageContent | undefined, newChunk: AIMessageContent): AIMessageContent;
    registerHandler<T extends AIMessageContent>(type: T['type'], // 使用类型中定义的type字段作为参数类型
    handler: (chunk: T, existing?: T) => T): void;
    private generateID;
    private registerDefaultHandlers;
    private createContentHandler;
    private registerTextHandlers;
    private registerThinkingHandler;
    private registerImageHandler;
    private registerSearchHandler;
}
