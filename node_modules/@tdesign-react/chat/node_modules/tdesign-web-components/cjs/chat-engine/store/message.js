/**
 * TDesign Web Components v1.3.0-alpha.2
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var toConsumableArray = require('../../_chunks/dep-d26a9931.js');
var defineProperty = require('../../_chunks/dep-f5b25212.js');
var createClass = require('../../_chunks/dep-2cbda8d4.js');
var inherits = require('../../_chunks/dep-732f8010.js');
var get = require('../../_chunks/dep-4075c7cc.js');
var chatEngine_utils_index = require('../utils/index.js');
var chatEngine_store_reactiveState = require('./reactiveState.js');
require('../../_chunks/dep-f8c29094.js');
require('../../_chunks/dep-ee0658df.js');
require('../../_chunks/dep-b75e0233.js');
require('../utils/immutable-patch.js');
require('../../_chunks/dep-9068f70e.js');
require('../../_chunks/dep-5b845a12.js');
require('../../_chunks/dep-a1c50fba.js');
require('immer');

function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { defineProperty._defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _callSuper(t, o, e) { return o = inherits._getPrototypeOf(o), inherits._possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], inherits._getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
var MessageStore = /*#__PURE__*/function (_ReactiveState) {
  function MessageStore() {
    createClass._classCallCheck(this, MessageStore);
    return _callSuper(this, MessageStore, arguments);
  }
  inherits._inherits(MessageStore, _ReactiveState);
  return createClass._createClass(MessageStore, [{
    key: "initialize",
    value: function initialize(initialState) {
      get._get(inherits._getPrototypeOf(MessageStore.prototype), "initialize", this).call(this, _objectSpread({
        messageIds: [],
        messages: []
      }, initialState));
    }
  }, {
    key: "createMessage",
    value: function createMessage(message) {
      var id = message.id;
      this.setState(function (draft) {
        draft.messageIds.push(id);
        draft.messages.push(message);
      });
    }
  }, {
    key: "createMultiMessages",
    value: function createMultiMessages(messages) {
      this.setState(function (draft) {
        var _draft$messages;
        messages.forEach(function (msg) {
          draft.messageIds.push(msg.id);
        });
        (_draft$messages = draft.messages).push.apply(_draft$messages, toConsumableArray._toConsumableArray(messages));
      });
    }
  }, {
    key: "setMessages",
    value: function setMessages(messages) {
      var mode = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "replace";
      this.setState(function (draft) {
        if (mode === "replace") {
          draft.messageIds = messages.map(function (msg) {
            return msg.id;
          });
          draft.messages = toConsumableArray._toConsumableArray(messages);
        } else if (mode === "prepend") {
          draft.messageIds = [].concat(toConsumableArray._toConsumableArray(messages.map(function (msg) {
            return msg.id;
          })), toConsumableArray._toConsumableArray(draft.messageIds));
          draft.messages = [].concat(toConsumableArray._toConsumableArray(messages), toConsumableArray._toConsumableArray(draft.messages));
        } else {
          var _draft$messageIds, _draft$messages2;
          (_draft$messageIds = draft.messageIds).push.apply(_draft$messageIds, toConsumableArray._toConsumableArray(messages.map(function (msg) {
            return msg.id;
          })));
          (_draft$messages2 = draft.messages).push.apply(_draft$messages2, toConsumableArray._toConsumableArray(messages));
        }
      });
    }
    // 追加内容到指定类型的content
  }, {
    key: "appendContent",
    value: function appendContent(messageId, processedContent) {
      var targetIndex = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : -1;
      this.setState(function (draft) {
        var message = draft.messages.find(function (m) {
          return m.id === messageId;
        });
        if (!message || !chatEngine_utils_index.isAIMessage(message) || !message.content) return;
        if (targetIndex >= 0 && targetIndex < message.content.length) {
          message.content[targetIndex] = processedContent;
        } else {
          message.content.push(processedContent);
        }
      });
    }
    // 完整替换消息的content数组
  }, {
    key: "replaceContent",
    value: function replaceContent(messageId, processedContent) {
      this.setState(function (draft) {
        var message = draft.messages.find(function (m) {
          return m.id === messageId;
        });
        if (!message || !chatEngine_utils_index.isAIMessage(message)) return;
        message.content = processedContent;
      });
    }
    // 更新消息整体状态
  }, {
    key: "setMessageStatus",
    value: function setMessageStatus(messageId, status) {
      this.setState(function (draft) {
        var message = draft.messages.find(function (m) {
          return m.id === messageId;
        });
        if (message) {
          message.status = status;
          if (chatEngine_utils_index.isAIMessage(message) && message.content && message.content.length > 0) {
            var lastContent = message.content[message.content.length - 1];
            if ("status" in lastContent && lastContent.status !== "error") {
              lastContent.status = status;
            }
          }
        }
      });
    }
    // 为消息设置扩展属性
  }, {
    key: "setMessageExt",
    value: function setMessageExt(messageId) {
      var attr = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
      this.setState(function (draft) {
        var message = draft.messages.find(function (m) {
          return m.id === messageId;
        });
        if (message) {
          message.ext = _objectSpread(_objectSpread({}, message.ext), attr);
        }
      });
    }
    // 为AI消息设置工具调用
    // setMessageToolCalls(messageId: string, toolCalls: ToolCall[]) {
    //   this.setState((draft) => {
    //     const message = draft.messages.find((m) => m.id === messageId);
    //     if (message && isAIMessage(message)) {
    //       message.toolCalls = toolCalls;
    //     }
    //   });
    // }
  }, {
    key: "clearHistory",
    value: function clearHistory() {
      this.setState(function (draft) {
        draft.messageIds = [];
        draft.messages = [];
      });
    }
    // 删除指定消息
  }, {
    key: "removeMessage",
    value: function removeMessage(messageId) {
      this.setState(function (draft) {
        var idIndex = draft.messageIds.indexOf(messageId);
        if (idIndex !== -1) {
          draft.messageIds.splice(idIndex, 1);
        }
        draft.messages = draft.messages.filter(function (msg) {
          return msg.id !== messageId;
        });
      });
    }
    // 创建消息分支（用于保留历史版本）
  }, {
    key: "createMessageBranch",
    value: function createMessageBranch(messageId) {
      var original = this.getState().messages.find(function (m) {
        return m.id === messageId;
      });
      if (!original || !original.content) return;
      var branchedMessage = _objectSpread(_objectSpread({}, original), {}, {
        content: original.content.map(function (c) {
          return _objectSpread({}, c);
        })
      });
      this.createMessage(branchedMessage);
    }
  }, {
    key: "messages",
    get: function get() {
      return this.getState().messages;
    }
  }, {
    key: "getMessageByID",
    value: function getMessageByID(id) {
      return this.getState().messages.find(function (m) {
        return m.id === id;
      });
    }
  }, {
    key: "currentMessage",
    get: function get() {
      var _this$getState = this.getState(),
        messages = _this$getState.messages;
      return messages[messages.length - 1];
    }
  }, {
    key: "lastAIMessage",
    get: function get() {
      var _this$getState2 = this.getState(),
        messages = _this$getState2.messages;
      var aiMessages = messages.filter(function (msg) {
        return chatEngine_utils_index.isAIMessage(msg);
      });
      return aiMessages[aiMessages.length - 1];
    }
  }, {
    key: "lastUserMessage",
    get: function get() {
      var _this$getState3 = this.getState(),
        messages = _this$getState3.messages;
      var userMessages = messages.filter(function (msg) {
        return chatEngine_utils_index.isUserMessage(msg);
      });
      return userMessages[userMessages.length - 1];
    }
    // 更新消息整体状态（自动推断）
  }, {
    key: "updateMessageStatusByContent",
    value: function updateMessageStatusByContent(message) {
      if (!message.content) return;
      if (message.content.some(function (c) {
        return c.status === "error";
      })) {
        message.status = "error";
        message.content.forEach(function (content) {
          if (content.status) {
            var resolvedStatus = content.status || "streaming";
            content.status = resolvedStatus === "streaming" ? "stop" : content.status;
          }
        });
        return;
      }
      message.content.slice(0, -1).forEach(function (content) {
        content.status = content.status !== "error" && content.status !== "stop" ? "complete" : content.status;
      });
    }
    /**
     * 更新多个内容块
     * @param messageId 消息ID
     * @param contents 要更新的内容块数组
     */
  }, {
    key: "updateMultipleContents",
    value: function updateMultipleContents(messageId, contents) {
      var _this = this;
      this.setState(function (draft) {
        var message = draft.messages.find(function (m) {
          return m.id === messageId;
        });
        if (!message || !chatEngine_utils_index.isAIMessage(message) || !message.content) return;
        var messageContent = message.content;
        contents.forEach(function (content) {
          var existingIndex = messageContent.findIndex(function (c) {
            return c.id === content.id || c.type === content.type;
          });
          if (existingIndex >= 0) {
            messageContent[existingIndex] = _objectSpread(_objectSpread({}, messageContent[existingIndex]), content);
          } else {
            messageContent.push(content);
          }
        });
        _this.updateMessageStatusByContent(message);
      });
    }
  }]);
}(chatEngine_store_reactiveState["default"]);

exports.MessageStore = MessageStore;
//# sourceMappingURL=message.js.map
