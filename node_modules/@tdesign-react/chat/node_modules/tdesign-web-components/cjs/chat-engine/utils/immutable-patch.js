/**
 * TDesign Web Components v1.3.0-alpha.2
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _typeof = require('../../_chunks/dep-b75e0233.js');
var objectWithoutProperties = require('../../_chunks/dep-9068f70e.js');
var defineProperty = require('../../_chunks/dep-f5b25212.js');
var toConsumableArray = require('../../_chunks/dep-d26a9931.js');
var nonIterableRest = require('../../_chunks/dep-5b845a12.js');
var unsupportedIterableToArray = require('../../_chunks/dep-f8c29094.js');
require('../../_chunks/dep-ee0658df.js');

function _toArray(r) {
  return nonIterableRest._arrayWithHoles(r) || toConsumableArray._iterableToArray(r) || unsupportedIterableToArray._unsupportedIterableToArray(r) || nonIterableRest._nonIterableRest();
}

function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof._typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof._typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof._typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { defineProperty._defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function parsePath(path) {
  if (path === "" || path === "/") return [];
  return path.split("/").slice(1).map(function (s) {
    return s.replace(/~1/g, "/").replace(/~0/g, "~");
  });
}
function getByPath(obj, path) {
  var current = obj;
  var _iterator = _createForOfIteratorHelper(path),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var key = _step.value;
      if (current == null) return void 0;
      current = current[key];
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return current;
}
function setByPath(obj, path, value) {
  if (path.length === 0) {
    return value;
  }
  var _path = _toArray(path),
    head = _path[0],
    tail = _path.slice(1);
  var current = obj;
  var isArray = Array.isArray(current);
  var index = isArray ? head === "-" ? current.length : parseInt(head, 10) : head;
  if (tail.length === 0) {
    if (isArray) {
      var newArr = toConsumableArray._toConsumableArray(current);
      newArr[index] = value;
      return newArr;
    }
    return _objectSpread(_objectSpread({}, current), {}, defineProperty._defineProperty({}, head, value));
  }
  var child = current[head];
  var newChild = setByPath(child, tail, value);
  if (isArray) {
    var _newArr = toConsumableArray._toConsumableArray(current);
    _newArr[index] = newChild;
    return _newArr;
  }
  return _objectSpread(_objectSpread({}, current), {}, defineProperty._defineProperty({}, head, newChild));
}
function removeByPath(obj, path) {
  if (path.length === 0) {
    return void 0;
  }
  var _path2 = _toArray(path),
    head = _path2[0],
    tail = _path2.slice(1);
  var current = obj;
  var isArray = Array.isArray(current);
  if (tail.length === 0) {
    if (isArray) {
      var index = parseInt(head, 10);
      var newArr = toConsumableArray._toConsumableArray(current);
      newArr.splice(index, 1);
      return newArr;
    }
    current[head];
      var rest = objectWithoutProperties._objectWithoutProperties(current, [head].map(_toPropertyKey));
    return rest;
  }
  var child = current[head];
  var newChild = removeByPath(child, tail);
  if (isArray) {
    var _index = parseInt(head, 10);
    var _newArr2 = toConsumableArray._toConsumableArray(current);
    _newArr2[_index] = newChild;
    return _newArr2;
  }
  return _objectSpread(_objectSpread({}, current), {}, defineProperty._defineProperty({}, head, newChild));
}
function applyOperationImmutable(document, operation) {
  var path = parsePath(operation.path);
  switch (operation.op) {
    case "add":
    case "replace":
      return setByPath(document, path, operation.value);
    case "remove":
      return removeByPath(document, path);
    case "append":
      {
        var existing = getByPath(document, path);
        var newValue = existing == null ? operation.value : String(existing) + operation.value;
        return setByPath(document, path, newValue);
      }
    case "move":
      {
        var fromPath = parsePath(operation.from);
        var value = getByPath(document, fromPath);
        var afterRemove = removeByPath(document, fromPath);
        return setByPath(afterRemove, path, value);
      }
    case "copy":
      {
        var _fromPath = parsePath(operation.from);
        var _value = getByPath(document, _fromPath);
        return setByPath(document, path, JSON.parse(JSON.stringify(_value)));
      }
    default:
      return document;
  }
}
function applyPatchImmutable(document, patch) {
  return patch.reduce(function (doc, op) {
    return applyOperationImmutable(doc, op);
  }, document);
}
function applyPatch(document, patch) {
  return {
    newDocument: applyPatchImmutable(document, patch)
  };
}

exports.applyPatch = applyPatch;
exports.applyPatchImmutable = applyPatchImmutable;
//# sourceMappingURL=immutable-patch.js.map
