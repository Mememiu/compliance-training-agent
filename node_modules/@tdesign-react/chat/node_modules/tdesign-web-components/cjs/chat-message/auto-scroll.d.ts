import 'tdesign-icons-web-components/esm/components/check-circle';
import 'tdesign-icons-web-components/esm/components/close-circle';
import { Component } from 'omi';
export interface TdAutoScrollProps {
    maxHeight?: number | string;
    /** 首次加载是否滚到底部，默认false */
    defaultScrollToBottom?: boolean;
}
export default class ChatThinking extends Component<TdAutoScrollProps> {
    static css: any[];
    static propTypes: {
        maxHeight: (NumberConstructor | StringConstructor)[];
        defaultScrollToBottom: BooleanConstructor;
    };
    private scrollRef;
    private innerRef;
    private scrollTopTmp;
    private observer;
    private scrollHeightTmp;
    /** 主动滚动产生的阻止自动滚动标记 */
    private preventAutoScroll;
    /**
     * 当前是否自动滚动
     * 设为false初始化数据不触发滚动，否则进来就滚到底部了
     */
    private isAutoScrollEnabled;
    private scrollToBottom;
    /** 触发自动滚动 */
    private handleAutoScroll;
    /** 检测自动滚动是否触发 */
    private checkAutoScroll;
    private handleScroll;
    install(): void;
    ready(): void;
    uninstall(): void;
    render(props: TdAutoScrollProps): JSX.Element;
}
