import { Component, OmiProps } from 'omi';
import { type StyledProps } from '../common';
import { type TdDividerProps } from './type';
export interface DividerProps extends TdDividerProps, StyledProps {
}
export default class Divider extends Component<DividerProps> {
    static css: string[];
    static defaultProps: {
        align: string;
        dashed: boolean;
        layout: string;
    };
    static propTypes: {
        align: StringConstructor;
        content: ObjectConstructor;
        dashed: BooleanConstructor;
        layout: StringConstructor;
    };
    componentName: string;
    render(props: OmiProps<DividerProps>): JSX.Element;
}
