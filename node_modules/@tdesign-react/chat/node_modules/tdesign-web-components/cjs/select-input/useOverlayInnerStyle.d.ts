import { Component } from 'omi';
import { PopupVisibleChangeContext } from '../popup';
import { TdSelectInputProps } from './type';
export type overlayStyleProps = Pick<TdSelectInputProps, 'popupProps' | 'readonly' | 'onPopupVisibleChange' | 'disabled' | 'allowInput' | 'popupVisible' | 'defaultPopupVisible'> & {
    /** 下拉框宽度是否与触发元素宽度保持一致，默认true */
    popupMatchWidth?: boolean;
};
export default function useOverlayInnerStyle(props: overlayStyleProps, extra?: {
    afterHidePopup?: (ctx: PopupVisibleChangeContext) => void;
}, activeComponent?: Component): {
    tOverlayInnerStyle: () => import("../common").Styles | ((triggerElement: HTMLElement, popupElement: HTMLElement) => import("../common").Styles);
    innerPopupVisible: boolean | import("omi").SignalValue<boolean>;
    onInnerPopupVisibleChange: (visible: boolean, context: PopupVisibleChangeContext) => void;
};
