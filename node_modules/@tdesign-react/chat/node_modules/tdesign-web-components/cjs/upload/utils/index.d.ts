import { SignalValue } from 'omi';
export declare const toSignal: <T extends Record<string, any>>(props: T) => { [K in keyof T]?: SignalValue<T[K]>; };
export declare const toRef: <T extends Record<string | symbol, any>>(props: SignalValue<T>) => { [K in keyof T]?: SignalValue<T[K]>; };
export declare const t: <T>(pattern: T, ...args: any[]) => string;
