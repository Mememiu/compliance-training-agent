import 'tdesign-icons-web-components/esm/components/arrow-down';
import '../chat-message';
import { Component } from 'omi';
import type { TdChatListProps, TdChatListScrollToOptions } from './type';
export default class Chatlist extends Component<TdChatListProps> {
    static css: any[];
    static propTypes: {
        autoScroll: BooleanConstructor;
        defaultScrollTo: StringConstructor;
        onScroll: FunctionConstructor;
    };
    static defaultProps: {
        autoScroll: boolean;
        defaultScrollTo: string;
    };
    private listRef;
    private innerRef;
    private scrollTopTmp;
    private scrollHeightTmp;
    private observer;
    /** 主动滚动产生的阻止自动滚动标记 */
    private preventAutoScroll;
    /**
     * 当前是否自动滚动
     * 设为false初始化数据不触发滚动，否则进来就滚到底部了
     */
    private isAutoScrollEnabled;
    scrollButtonVisible: import("omi").SignalValue<boolean>;
    /** 触发自动滚动 */
    private handleAutoScroll;
    /** 检测自动滚动是否触发 */
    private checkAutoScroll;
    /** 检测并显示滚到底部按钮 */
    private checkAndShowScrollButton;
    private handleScroll;
    ready(): void;
    uninstall(): void;
    render(): JSX.Element;
    scrollList(options?: TdChatListScrollToOptions): void;
}
