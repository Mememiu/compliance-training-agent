import EventEmitter from '../utils/eventEmitter';
/**
 * 批量请求客户端（非流式）
 */
export declare class BatchClient extends EventEmitter {
    private controller;
    private logger;
    /**
     * 发送批量请求
     * @param endpoint API端点
     * @param request 请求参数
     * @param timeout 超时时间（毫秒）
     * @returns 响应数据
     */
    request<T>(endpoint: string, request: RequestInit, timeout?: number): Promise<T>;
    /**
     * 中止当前请求
     */
    abort(): void;
}
