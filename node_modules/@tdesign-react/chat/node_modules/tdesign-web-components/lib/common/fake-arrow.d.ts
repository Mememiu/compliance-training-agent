import { Component, OmiProps } from 'omi';
/**
 * 展开图标方向
 */
export type ArrowDirection = 'up' | 'down' | 'left' | 'right';
export interface FakeArrowProps {
    /**
     * 是否为激活态
     */
    isActive?: boolean;
    /**
     * 是否禁用
     */
    disabled?: boolean;
    /**
     * 自定义样式
     */
    style?: Partial<CSSStyleDeclaration>;
    /**
     * 箭头初始方向
     * @default down
     */
    direction?: ArrowDirection;
    /**
     * 激活时的箭头方向，仅在设置了 direction 属性时生效
     * @default up
     */
    activeDirection?: ArrowDirection;
}
export default class FakeArrow extends Component<FakeArrowProps> {
    componentName: string;
    static propTypes: {
        isActive: BooleanConstructor;
        disabled: BooleanConstructor;
        style: ObjectConstructor;
        direction: StringConstructor;
        activeDirection: StringConstructor;
    };
    static css: string;
    render(props: OmiProps<FakeArrowProps>): JSX.Element;
}
