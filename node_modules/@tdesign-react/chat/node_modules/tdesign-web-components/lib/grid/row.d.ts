import { Component } from 'omi';
import { StyledProps, TNode } from '../common';
import { TdRowProps } from './type';
/**
 * Row 组件支持的属性。
 */
export interface RowProps extends TdRowProps, StyledProps {
    /**
     * 默认子元素内容
     */
    children: TNode;
}
export default class Row extends Component<RowProps> {
    static defaultProps: TdRowProps;
    static propTypes: {
        align: StringConstructor;
        gutter: (ObjectConstructor | ArrayConstructor | NumberConstructor)[];
        justify: StringConstructor;
        tag: StringConstructor;
    };
    size: string;
    provide: {
        size: string;
        gutter: number | import("./type").GutterObject | (number | import("./type").GutterObject)[];
    };
    updateSize: () => void;
    install(): void;
    uninstall(): void;
    render(props: RowProps): import("omi").VNode<any>;
}
