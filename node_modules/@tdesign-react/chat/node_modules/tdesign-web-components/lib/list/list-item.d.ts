import '../loading/loading';
import { Component, OmiProps } from 'omi';
import { ListItemProps } from './types';
export default class ListItem extends Component<ListItemProps> {
    static css: any[];
    static propTypes: {
        action: (ObjectConstructor | NumberConstructor | FunctionConstructor | StringConstructor)[];
        content: (ObjectConstructor | NumberConstructor | FunctionConstructor | StringConstructor)[];
        onClick: FunctionConstructor;
    };
    static isLightDOM: boolean;
    static defaultProps: {};
    render(props: OmiProps<ListItemProps>): JSX.Element;
}
