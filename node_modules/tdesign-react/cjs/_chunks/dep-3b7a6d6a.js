/**
 * tdesign v1.18.0
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

var _baseGetTag = require('./dep-0699ecec.js');
var isArray = require('./dep-ed2c0863.js');
var isObjectLike = require('./dep-703570ca.js');

/** `Object#toString` result references. */
var stringTag = '[object String]';

/**
 * Checks if `value` is classified as a `String` primitive or object.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a string, else `false`.
 * @example
 *
 * _.isString('abc');
 * // => true
 *
 * _.isString(1);
 * // => false
 */
function isString(value) {
  return typeof value == 'string' || !isArray.isArray(value) && isObjectLike.isObjectLike(value) && _baseGetTag.baseGetTag(value) == stringTag;
}

exports.isString = isString;
//# sourceMappingURL=dep-3b7a6d6a.js.map
