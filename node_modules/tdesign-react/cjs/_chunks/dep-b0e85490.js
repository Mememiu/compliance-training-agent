/**
 * tdesign v1.18.0
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

var _baseGet = require('./dep-c30bb241.js');
var isArguments = require('./dep-9e21e877.js');
var isArray = require('./dep-ed2c0863.js');
var _isIndex = require('./dep-f499d04a.js');
var isLength = require('./dep-20ffab37.js');

/**
 * Checks if `path` exists on `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {Array|string} path The path to check.
 * @param {Function} hasFunc The function to check properties.
 * @returns {boolean} Returns `true` if `path` exists, else `false`.
 */
function hasPath(object, path, hasFunc) {
  path = _baseGet.castPath(path, object);
  var index = -1,
    length = path.length,
    result = false;
  while (++index < length) {
    var key = _baseGet.toKey(path[index]);
    if (!(result = object != null && hasFunc(object, key))) {
      break;
    }
    object = object[key];
  }
  if (result || ++index != length) {
    return result;
  }
  length = object == null ? 0 : object.length;
  return !!length && isLength.isLength(length) && _isIndex.isIndex(key, length) && (isArray.isArray(object) || isArguments.isArguments(object));
}

exports.hasPath = hasPath;
//# sourceMappingURL=dep-b0e85490.js.map
