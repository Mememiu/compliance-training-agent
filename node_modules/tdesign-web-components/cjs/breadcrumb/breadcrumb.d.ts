import './breadcrumb-item';
import { Component } from 'omi';
import { StyledProps } from '../common';
import { TdBreadcrumbProps } from './type';
interface BreadcrumbProps extends TdBreadcrumbProps, StyledProps {
}
export default class Breadcrumb extends Component<BreadcrumbProps> {
    className: string;
    static propTypes: {
        maxItemWidth: StringConstructor;
        options: ObjectConstructor;
        separator: (ObjectConstructor | FunctionConstructor | StringConstructor | NumberConstructor)[];
    };
    get contentNodes(): any[];
    provide: {
        tBreadcrumb: {
            separator: string;
            maxItemWidth: string;
        };
    };
    beforeRender(): void;
    render(): JSX.Element;
}
export {};
