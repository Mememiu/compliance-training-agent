export default class SSEClient {
    private url;
    private handlers;
    private options;
    private controller;
    private retries;
    private reader;
    private config;
    private buffer;
    private isClosed;
    constructor(url: string, handlers: {
        onMessage?: (data: any) => void;
        onError?: (error: Error | Response) => void;
        onComplete?: (isAborted: boolean) => void;
    }, options?: {
        retryInterval?: number;
        maxRetries?: number;
    });
    connect(config: RequestInit): Promise<void>;
    close(): Promise<void>;
    private readStream;
    private parseChunk;
    private handleError;
    private reconnect;
}
