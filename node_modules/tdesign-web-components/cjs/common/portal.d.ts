import { Component } from 'omi';
import { AttachNode, AttachNodeReturnValue, TNode } from '../common';
export interface PortalProps {
    /**
     * 指定挂载的 HTML 节点, false 为挂载在 body
     */
    attach?: AttachNode;
    /**
     * 触发元素
     */
    triggerNode?: HTMLElement;
    children: TNode;
    onDOMReady?: (h: Element) => void;
}
export declare function getAttach(attach: PortalProps['attach'], triggerNode?: HTMLElement): AttachNodeReturnValue;
export default class Portal extends Component<PortalProps> {
    static css: any[];
    static isLightDOM: boolean;
    static propTypes: {
        attach: (ObjectConstructor | FunctionConstructor | StringConstructor | BooleanConstructor)[];
        triggerNode: ObjectConstructor;
    };
    static defaultProps: {
        attach: boolean;
    };
    parentElement: HTMLElement | null;
    container: HTMLElement;
    classPrefix: any;
    getContainer(): HTMLDivElement;
    install(): void;
    uninstall(): void;
    ready(): void;
    render(): any;
}
