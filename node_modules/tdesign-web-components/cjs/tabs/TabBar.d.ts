import { Component } from 'omi';
import { StyledProps, Styles } from '../common';
import { TabValue } from './type';
interface TabBarProps extends StyledProps {
    tabPosition: string;
    activeId: TabValue;
    containerRef: Object;
}
export default class TabBar extends Component<TabBarProps> {
    static defaultProps: {
        tabPosition: string;
        activeId: string;
    };
    static propsType: {
        tabPosition: StringConstructor;
        activeId: (StringConstructor | NumberConstructor)[];
        containerRef: ObjectConstructor;
    };
    currentActiveIdRef: Partial<Record<"current", TabValue>>;
    barStyle: Omi.SignalValue<Styles>;
    tabsClassPrefix: string;
    containerRef: Partial<Record<"current", HTMLDivElement>>;
    computeStyle(): void;
    install(): void;
    installed(): void;
    render(): JSX.Element;
}
export {};
