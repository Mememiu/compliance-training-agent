/**
 * TDesign Web Components v1.1.10
 * (c) 2025 tdesign
 * @license MIT
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var lodashEs = require('lodash-es');
var omi = require('omi');

var toSignal = function toSignal(props) {
  var reactiveObject = {};
  Object.keys(props).forEach(function (key) {
    var propKey = key;
    reactiveObject[propKey] = omi.signal(props[propKey]);
  });
  return reactiveObject;
};
var toRef = function toRef(props) {
  var reactiveObject = {};
  Object.keys(props.value).forEach(function (key) {
    var propKey = key;
    reactiveObject[propKey] = omi.computed(function () {
      return props.value[propKey];
    });
  });
  return new Proxy(reactiveObject, {
    get: function get(target, prop) {
      return target[prop] ? target[prop] : omi.signal(void 0);
    }
  });
};
var t = function t(pattern) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  var data = args[0];
  if (lodashEs.isString(pattern)) {
    if (!data) return pattern;
    var regular = /\{\s*([\w-]+)\s*\}/g;
    var translated = pattern.replace(regular, function (match, key) {
      if (data) {
        return String(data[key]);
      }
      return "";
    });
    return translated;
  }
  if (lodashEs.isFunction(pattern)) {
    console.error("NOT IMPLEMENTED");
    return "";
  }
  return "";
};

exports.t = t;
exports.toRef = toRef;
exports.toSignal = toSignal;
//# sourceMappingURL=index.js.map
