/**
 * TDesign Web Components v1.1.10
 * (c) 2025 tdesign
 * @license MIT
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var lodashEs = require('lodash-es');

var toLowercaseSeparator = function toLowercaseSeparator(key) {
  return key.replace(/([A-Z])/g, "-$1").toLowerCase();
};
var getStyleStr = function getStyleStr(style) {
  return Object.keys(style).map(function (key) {
    return "".concat(toLowercaseSeparator(key), ": ").concat(style[key], ";");
  }).join(" ");
};
var DEFAULT_OPTIONS = {
  debounceTime: 0,
  config: {
    attributes: true,
    childList: true,
    characterData: true,
    subtree: true
  }
};
var createMutationObservable = function createMutationObservable(targetEl, cb) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : DEFAULT_OPTIONS;
  var debounceTime = options.debounceTime,
    config = options.config;
  var observer = new MutationObserver(debounceTime > 0 ? lodashEs.debounce(cb, debounceTime) : cb);
  var targetNode = targetEl || document.body;
  observer.observe(targetNode, config);
  return function () {
    observer.disconnect();
  };
};

exports.createMutationObservable = createMutationObservable;
exports.getStyleStr = getStyleStr;
//# sourceMappingURL=utils.js.map
