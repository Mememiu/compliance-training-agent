/**
 * TDesign Web Components v1.1.10
 * (c) 2025 tdesign
 * @license MIT
 */

import markdownIt from 'markdown-it';

function _createForOfIteratorHelper(r, e) {

 throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) {

function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function markdownToTextWithParser(markdown) {
  var md = markdownIt({
    html: false,
    linkify: false
  });
  var tokens = md.parse(markdown, {});
  var text = "";
  var _iterator = _createForOfIteratorHelper(tokens),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var token = _step.value;
      if (token.type === "inline" && token.children) {
        text += token.children.filter(function (t) {
          return t.type === "text" || t.type === "code_inline";
        }).map(function (t) {
          return t.content;
        }).join("");
      } else if (token.type === "text" || token.type === "code_block") {
        text += token.content;
      }
      if (token.type === "paragraph_close") {
        text += "\n";
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return text.replace(/\n{2,}/g, "\n").trim();
}
function mdPartAttrPlugin(md) {
  var originalRenderToken = md.renderer.renderToken.bind(md.renderer);
  var prefix = "md_";
  md.renderer.renderToken = function (tokens, idx, options) {
    var token = tokens[idx];
    if (token.nesting === 1 || token.nesting === 0) {
      if (!token.attrs) token.attrs = [];
      var partValue = token.type;
      if (token.type === "heading_open") {
        partValue = token.tag;
      }
      token.attrs.push(["part", "".concat(prefix).concat(partValue.replace(/_open$/, ""))]);
    }
    return originalRenderToken(tokens, idx, options);
  };
  md.renderer.rules.code_inline = function (tokens, idx, options, env, self) {
    var token = tokens[idx];
    token.attrs = token.attrs || [];
    token.attrs.push(["part", "".concat(prefix, "code_inline")]);
    return "".concat(self.renderToken(tokens, idx, options) + md.utils.escapeHtml(token.content), "</code>");
  };
  var origCodeBlock = md.renderer.rules.code_block || function (tokens, idx, options, env, self) {
    return "".concat(self.renderToken(tokens, idx, options) + md.utils.escapeHtml(tokens[idx].content), "</code></pre>\n");
  };
  md.renderer.rules.code_block = function (tokens, idx, options, env, self) {
    var html = origCodeBlock(tokens, idx, options, env, self);
    html = html.replace("<pre", "<pre part=\"".concat(prefix, "pre_block\""));
    html = html.replace("<code", "<code part=\"".concat(prefix, "code_block\""));
    return html;
  };
  var origFence = md.renderer.rules.fence || function (tokens, idx, options, env, self) {
    return "".concat(self.renderToken(tokens, idx, options) + md.utils.escapeHtml(tokens[idx].content), "</code></pre>\n");
  };
  md.renderer.rules.fence = function (tokens, idx, options, env, self) {
    var html = origFence(tokens, idx, options, env, self);
    html = html.replace("<pre", "<pre part=\"".concat(prefix, "fence\""));
    html = html.replace("<code", "<code part=\"".concat(prefix, "code\""));
    return html;
  };
}

export { markdownToTextWithParser, mdPartAttrPlugin };
//# sourceMappingURL=utils.js.map
