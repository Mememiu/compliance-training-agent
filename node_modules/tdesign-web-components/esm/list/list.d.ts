import '../loading/loading';
import { Component, OmiProps } from 'omi';
import { ListProps } from './types';
export default class BackTop extends Component<ListProps> {
    static css: any[];
    static defaultProps: {
        layout: string;
        size: string;
        split: boolean;
        stripe: boolean;
    };
    static propTypes: {
        split: BooleanConstructor;
        stripe: BooleanConstructor;
        dragSort: BooleanConstructor;
        asyncLoading: StringConstructor;
        footer: StringConstructor;
        header: StringConstructor;
        children: StringConstructor;
        layout: StringConstructor;
        size: StringConstructor;
        onLoadMore: FunctionConstructor;
        onScroll: FunctionConstructor;
    };
    getClasses(split: Boolean, stripe: Boolean): string[];
    renderLoadElement(asyncLoading: string): JSX.Element;
    handleClickLoad: (e: Event, asyncLoading: string) => void;
    renderHeader(header: any): JSX.Element;
    renderFooter(footer: any): JSX.Element;
    render(props: OmiProps<ListProps>): JSX.Element;
}
