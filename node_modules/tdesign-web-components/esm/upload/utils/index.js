/**
 * TDesign Web Components v1.1.10
 * (c) 2025 tdesign
 * @license MIT
 */

import { isString, isFunction } from 'lodash-es';
import { signal, computed } from 'omi';

var toSignal = function toSignal(props) {
  var reactiveObject = {};
  Object.keys(props).forEach(function (key) {
    var propKey = key;
    reactiveObject[propKey] = signal(props[propKey]);
  });
  return reactiveObject;
};
var toRef = function toRef(props) {
  var reactiveObject = {};
  Object.keys(props.value).forEach(function (key) {
    var propKey = key;
    reactiveObject[propKey] = computed(function () {
      return props.value[propKey];
    });
  });
  return new Proxy(reactiveObject, {
    get: function get(target, prop) {
      return target[prop] ? target[prop] : signal(void 0);
    }
  });
};
var t = function t(pattern) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  var data = args[0];
  if (isString(pattern)) {
    if (!data) return pattern;
    var regular = /\{\s*([\w-]+)\s*\}/g;
    var translated = pattern.replace(regular, function (match, key) {
      if (data) {
        return String(data[key]);
      }
      return "";
    });
    return translated;
  }
  if (isFunction(pattern)) {
    console.error("NOT IMPLEMENTED");
    return "";
  }
  return "";
};

export { t, toRef, toSignal };
//# sourceMappingURL=index.js.map
