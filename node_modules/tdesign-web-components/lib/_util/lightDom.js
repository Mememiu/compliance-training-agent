/**
 * TDesign Web Components v1.1.10
 * (c) 2025 tdesign
 * @license MIT
 */

import { _ as _createClass, a as _classCallCheck } from '../_chunks/dep-f3b89a13.js';
import { _ as _inherits, a as _getPrototypeOf, b as _possibleConstructorReturn } from '../_chunks/dep-78b2121e.js';
import { _ as _get } from '../_chunks/dep-fac20cec.js';
import { _ as _defineProperty } from '../_chunks/dep-65412e96.js';
import { _ as _typeof } from '../_chunks/dep-eb45a3ed.js';
import { define } from 'omi';
import parseTNode from './parseTNode.js';
import '../_chunks/dep-dd627c5f.js';
import 'lodash-es';
import '../_common/js/log/log.js';

function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
var createStyleSheet = function createStyleSheet(style) {
  var styleSheet = new CSSStyleSheet();
  styleSheet.replaceSync(style);
  Object.defineProperty(styleSheet, "styleStr", {
    get: function get() {
      return style;
    }
  });
  return styleSheet;
};
var getCssList = function getCssList(css) {
  if (Array.isArray(css)) {
    return css.map(function (item) {
      return getCssList(item);
    }).flat();
  }
  if (_typeof(css) === "object" && typeof css["default"] === "string") {
    return [css["default"]];
  }
  if (typeof css === "string") {
    return [css];
  }
  return [];
};
var findParentRenderRoot = function findParentRenderRoot(ele) {
  return ele.getRootNode() || document;
};
var lightDomCtorCache = /* @__PURE__ */new Map();
var buildLightDomCtor = function buildLightDomCtor(nodeCtor) {
  var cacheCtor = lightDomCtorCache.get(nodeCtor);
  if (cacheCtor) {
    return cacheCtor;
  }
  var TNodeLightDom = /*#__PURE__*/function (_nodeCtor) {
    function TNodeLightDom() {
      _classCallCheck(this, TNodeLightDom);
      return _callSuper(this, TNodeLightDom, arguments);
    }
    _inherits(TNodeLightDom, _nodeCtor);
    return _createClass(TNodeLightDom, [{
      key: "beforeRender",
      value:
      /**
       * 处理原 ShadowRoot 的样式表
       * 合并到上层的 ShadowRoot | document 样式表中
       */
      function beforeRender() {
        var parentElement = findParentRenderRoot(this);
        var cssList = getCssList(nodeCtor.css);
        cssList.forEach(function (style) {
          var _parentElement$adopte;
          var preStyleSheet = (_parentElement$adopte = parentElement.adoptedStyleSheets) === null || _parentElement$adopte === void 0 ? void 0 : _parentElement$adopte.find(function (item) {
            return item.styleStr === style;
          });
          if (preStyleSheet) {
            return;
          }
          var styleSheet = createStyleSheet(style);
          if (parentElement.adoptedStyleSheets) {
            parentElement.adoptedStyleSheets.push(styleSheet);
          } else {
            parentElement.adoptedStyleSheets = [styleSheet];
          }
        });
        _get(_getPrototypeOf(TNodeLightDom.prototype), "beforeRender", this).call(this);
      }
    }]);
  }(nodeCtor);
  _defineProperty(TNodeLightDom, "isLightDOM", true);
  lightDomCtorCache.set(nodeCtor, TNodeLightDom);
  return TNodeLightDom;
};
var convertToLightDomNode = function convertToLightDomNode(node) {
  var tNode = parseTNode(node);
  if (!tNode) {
    return tNode;
  }
  if (!(_typeof(tNode) === "object" && "nodeName" in tNode && typeof tNode.nodeName === "string")) {
    return tNode;
  }
  var nodeCtor = customElements.get(tNode.nodeName);
  if (!(nodeCtor && nodeCtor.is === "Component" && !nodeCtor.isLightDOM)) {
    return tNode;
  }
  var lightDomCtor = buildLightDomCtor(nodeCtor);
  var lightDomNodeName = "".concat(tNode.nodeName, "-light-dom");
  if (!customElements.get(lightDomNodeName)) {
    define(lightDomNodeName, lightDomCtor);
  }
  return _objectSpread(_objectSpread({}, tNode), {}, {
    nodeName: lightDomNodeName
  });
};

export { convertToLightDomNode, createStyleSheet };
//# sourceMappingURL=lightDom.js.map
