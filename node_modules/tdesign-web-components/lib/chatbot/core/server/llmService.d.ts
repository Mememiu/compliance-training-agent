import type { AIMessageContent, ChatRequestParams, ChatServiceConfig } from '../type';
export interface ILLMService {
    /**
     * 处理批量请求（非流式）
     */
    handleBatchRequest(params: ChatRequestParams, config: ChatServiceConfig): Promise<AIMessageContent>;
    /**
     * 处理流式请求
     */
    handleStreamRequest(params: ChatRequestParams, config: ChatServiceConfig): Promise<void>;
}
export declare class LLMService implements ILLMService {
    private sseClient;
    private fetchAbortController;
    handleBatchRequest(params: ChatRequestParams, config: ChatServiceConfig): Promise<any>;
    /**
     * 中止当前正在进行的批量请求
     */
    closeFetch(): void;
    closeSSE(): void;
    handleStreamRequest(params: ChatRequestParams, config: ChatServiceConfig): Promise<void>;
}
