import { Component, OmiDOMAttributes } from 'omi';
import { StyledProps } from '../common';
import { MenuValue, TdMenuProps } from './type';
export interface MenuProps extends TdMenuProps, StyledProps, OmiDOMAttributes {
}
export default class Menu extends Component<MenuProps> {
    static css: any[];
    static defaultProps: TdMenuProps;
    static propTypes: {
        collapsed: BooleanConstructor;
        value: (StringConstructor | NumberConstructor)[];
        width: (ArrayConstructor | StringConstructor | NumberConstructor)[];
        onChange: FunctionConstructor;
    };
    private active;
    private menuCollapsed;
    provide: {
        collapsed: import("omi").SignalValue<boolean>;
        active: import("omi").SignalValue<MenuValue>;
        onChange: (value: MenuValue) => void;
    };
    handleChange(value: MenuValue): void;
    render(): JSX.Element;
}
